# BRO AI Assistant Dependencies
# ================================

# =============================================================================
# CORE AI (Ollama - Local)
# =============================================================================
# Ollama runs models locally - install from https://ollama.com
# Required models: ollama pull gemma3 && ollama pull llava:7b

# Memory System
chromadb>=0.4.0
sentence-transformers>=2.2.0

# TOML Support (Python 3.11+ has tomllib built-in)
tomli-w>=1.0.0  # For writing TOML files

# =============================================================================
# PC CONTROL & AUTOMATION
# =============================================================================
pyautogui>=0.9.54
psutil>=5.9.0

# =============================================================================
# VOICE (STT & TTS)
# =============================================================================
# Offline TTS
pyttsx3>=2.90

# Speech Recognition
SpeechRecognition>=3.10.0
pyaudio>=0.2.13

# Wake Word (Offline)
vosk>=0.3.45

# Local STT - Faster Whisper (5x faster than cloud, 100% private)
faster-whisper>=0.10.0
# Optional: torch for Silero VAD (voice activity detection)
# torch>=2.0.0

# =============================================================================
# VISION SYSTEM
# =============================================================================
Pillow>=10.0.0
opencv-python>=4.8.0

# OCR (Text Reading) - Highly Recommended
easyocr>=1.7.0

# =============================================================================
# ANDROID PHONE CONTROL
# =============================================================================
pure-python-adb>=0.3.0

# =============================================================================
# WEB AUTOMATION
# =============================================================================
playwright>=1.40.0
# After install, run: playwright install chromium

# =============================================================================
# FILE CONVERSION
# =============================================================================
PyPDF2>=3.0.0
python-pptx>=0.6.21
python-docx>=1.1.0

# =============================================================================
# UTILITIES
# =============================================================================
python-dotenv>=1.0.0
colorama>=0.4.6

# =============================================================================
# CALL SENTRY (Phone Screening) - Optional
# =============================================================================
fastapi>=0.100.0
uvicorn>=0.23.0
# twilio>=8.0.0  # Only if using Twilio for calls

# =============================================================================
# OPTIONAL - Enhanced Features
# =============================================================================
# google-generativeai>=0.8.0  # Gemini API (cloud fallback)
# edge-tts>=6.1.0  # High-quality Microsoft voices
# pytesseract>=0.3.10  # Alternative OCR (requires Tesseract)

# =============================================================================
# UNIVERSAL APP LAUNCHER - Enhanced App Discovery (Optional but Recommended)
# =============================================================================
# These packages enable better discovery and launching of installed apps:
winapps>=0.3.0  # Scans Windows registry for installed apps
AppOpener>=1.7  # Finds and opens apps by name
pywin32>=306    # Windows API for reading Start Menu shortcuts

